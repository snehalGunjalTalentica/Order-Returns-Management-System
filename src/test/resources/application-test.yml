spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  
  flyway:
    enabled: false
  
  batch:
    job:
      enabled: false

jwt:
  secret: TestSecretKeyForJWTTokenGeneration2024SecureKey
  expiration: 86400000

payment:
  gateway:
    url: http://localhost:8080/mock-payment-gateway/refund
    timeout: 5000

logging:
  level:
    com.articurated: INFO
    org.springframework: WARN


